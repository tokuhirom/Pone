language: perl
install:
  - curl -L http://cpanmin.us/ | perl - Capture::Tiny Test::Base
script:
  - PERL5LIB="$HOME/perl5/lib/perl5/" make test
addons:
  apt:
    packages:
      - valgrind
sudo: false

