language: perl
install:
  - cpanm -nv Capture::Tiny Test::Base
script:
  - cmake -DCMAKE_BUILD_TYPE=Debug .
  - make all
  - prove -v -r t xt
addons:
  apt:
    packages:
      - valgrind
      - cmake
sudo: false
os:
  - linux
  - osx

