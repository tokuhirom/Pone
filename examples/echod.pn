use inet io/socket/inet;
# TODO `use io/socket/inet;`

my $chan = chan(9);

async(sub {
    # TODO loop
    while True {
        my $csock = $chan.receive();
        for 1..10 {
            my $buf = $csock.read(1000);
            $csock.write($buf);
            say "done read";
        }
        say "CLOSE";
        $csock.close();
    }
});

my $sock = inet.listen("127.0.0.1", 10084);

# TODO listen or die
while True {
    my $csock = $sock.accept();
    $chan.send($csock);
}

